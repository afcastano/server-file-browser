FileBrowser
================
- Select individual items
- Connect to git
+ Set up simple password
+ Load the whole list
- Find a way to better read the path.
- Load the collapsed tree
- When a dir is empty, show the folder instead of the file.
- Control when lots of files are loaded. Maybe load only files and dirs and then load contents on demand.
+ Make it async
- Dont show hidden files
- Dont fail if it cant delete
+ Check if the file exists in the dest folder recursively and show msg.
+ Find a way to not include hidden files in copy recursive.
- Delete not empty dirs
+ Preserve collapsed status on session
+ Preserve collapsed status when default path is the same.
+ Make modal smaller
+ Avoid modal flickering